/** Clase base para inputs de una línea en formularios del admin. */
export const fieldClass = "h-9 rounded-md border border-border bg-input px-3 text-foreground outline-none focus:border-primary"

/** Clase para textareas (campos altos) en formularios del admin. */
export const tallFieldClass = "min-h-20 rounded-md border border-border bg-input px-3 py-2 text-foreground outline-none focus:border-primary"

/** Indentación de errores inline para alinearlos con el campo en pantallas sm+. */
export const errorIndentClass = "sm:pl-[152px]"
