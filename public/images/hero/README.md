# Hero Images

Put the home hero background image in this folder.

Expected filename:

- `hero-background.jpg`

You can use a `.png` or `.webp` instead, but then update the `src` in `src/components/home/hero-section.tsx`.

Recommended size: at least 1920px wide, landscape format.
